server:
  port: 8082

spring:
  application:
    name: analytics-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.cloudwatchr: INFO
    org.springframework: INFO