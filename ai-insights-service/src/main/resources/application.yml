server:
  port: 8084

spring:
  application:
    name: ai-insights-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.cloudwatchr.ai: INFO
    org.springframework: INFO